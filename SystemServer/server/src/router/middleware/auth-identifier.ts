import { Request, Router } from "express";
import { ObjectId } from "mongodb";
import { InstanceType } from "typegoose";

const router = Router();

router.use(async(req,res)=>{

});

export const identifierMiddleware = router;
